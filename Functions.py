# always start function with def and then function name
# def functionname():
# code has to be indented inside the function
# two or more words use underscore and use lowercase


def say_hi(name, age):
    return print("Hello " + name + " you are " + str(age))


# this calls the function
say_hi("Adi", 21)
say_hi("Kist", 21)
