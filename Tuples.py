coordinates = (4, 5)
# tuples are immutable meaning that you can't reassign a new
# value once it is created
# Lists allow you to reassign or add, delete, modify and change
# stuff while tuple doesn't
# tuples are meant for data that DOESN'T CHANGE

print(coordinates[0])
print(coordinates[1])
